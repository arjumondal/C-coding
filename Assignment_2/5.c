#include<stdio.h>#include<stdlib.h>int main(){	int time_period;	float deposit, interest;		printf("Enter The Deposit Amount:\t");	scanf("%f", &deposit);	printf("Enter The Time Period in MOnths:\t");	scanf("%d", &time_period);			if (time_period < 6)		interest = ((5 / 100) * deposit);	else if (time_period >= 6 && time_period > 12)		interest = ((6 / 100) * deposit);	else if (time_period >= 12 && time_period < (3 * 12))		interest = ((8 / 100) * deposit);	else if (time_period >= (3 * 12) && time_period < (5 * 12))		interest = ((9 / 100) * deposit);	else		interest = ((10 / 100) * deposit);		printf("Rs %f Interest Should be Paid by The Bank.", interest);}